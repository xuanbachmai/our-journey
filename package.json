{
  "name": "our-journey",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @hh/client",
    "build": "npm run build -w @hh/client",
    "preview": "npm run preview -w @hh/client",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck -w @hh/client"
  }
}
