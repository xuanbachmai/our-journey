{
  "name": "@hh/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "node scripts/make-icons.cjs && vite build",
    "preview": "vite preview --host",
    "test": "vitest run --configLoader runner",
    "typecheck": "tsc --noEmit",
    "icons": "node scripts/make-icons.cjs"
  },
  "dependencies": {
    "@hh/shared": "*",
    "@supabase/supabase-js": "^2.116.0",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
